---
commonOverlays:
  - name: "set the kubernetes and nukleros labels"
    query: "$"
    action: merge
    onMission:
      action: 
    value:
      metadata:
        labels:
          app: external-dns-route53
          app.kubernetes.io/name: external-dns-route53
          app.kubernetes.io/instance: external-dns-route53
          app.kubernetes.io/component: external-dns
          app.kubernetes.io/version: {{ .version }}
          app.kubernetes.io/part-of: external-dns
          app.kubernetes.io/managed-by: platform-component-library
          platform.nukleros.io/group: ingress
          platform.nukleros.io/project: external-dns
      spec:
        selector:
          matchLabels:
            app: external-dns-route53
        template:
          metadata:
            labels:
              app: external-dns-route53
              app.kubernetes.io/name: external-dns-route53
              app.kubernetes.io/instance: external-dns-route53
              app.kubernetes.io/component: external-dns
              app.kubernetes.io/version: {{ .version }}
              app.kubernetes.io/part-of: external-dns
              app.kubernetes.io/managed-by: platform-component-library
              platform.nukleros.io/group: ingress
              platform.nukleros.io/project: external-dns

yamlFiles:
  - name: "external-dns deployments"
    path: "../../vendor/external-dns-deployment.yaml"
    outputPath: "ingress/external-dns/deployment-route53.yaml"
    overlays:
      - name: "update deployment spec"
        query: $
        value:
          metadata:
            name: "{{ .names.route53 }}"
            namespace: "{{ .namespace }}"
          spec:
            replicas: !!int {{ .replicas }}
            template:
              spec:
                securityContext:
                  fsGroup: 1001
                  runAsUser: 1001
                  runAsGroup: 1001
                  runAsNonRoot: true
                nodeSelector:
                  kubernetes.io/os: linux
                affinity:
                  podAntiAffinity:
                    preferredDuringSchedulingIgnoredDuringExecution:
                      - weight: 100
                        podAffinityTerm:
                          topologyKey: kubernetes.io/hostname
                          labelSelector:
                            matchExpressions:
                              - key: app.kubernetes.io/name
                                operator: In
                                values:
                                  - "{{ .names.route53 }}"

      - name: "update container spec"
        query: spec.template.spec.containers[*]
        value:
          envFrom:
            - secretRef:
                name: external-dns-route53
          image: "{{ .image }}"
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: "{{ .resources.requests.cpu }}"
              memory: "{{ .resources.requests.memory }}"
            limits:
              cpu: "{{ .resources.limits.cpu }}"
              memory: "{{ .resources.limits.memory }}"
